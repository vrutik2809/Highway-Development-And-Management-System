import { container } from "../../nextjs-material-kit";

const tenderStyle = {
    boxContainer: {
        ...container,
        marginTop: "120px",
        marginBottom: "5px",
    },
}
export default tenderStyle;