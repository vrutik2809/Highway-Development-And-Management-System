import { container } from "../../nextjs-material-kit";

const noticeStyle = {
    boxContainer: {
        ...container,
        marginTop: "120px",
        marginBottom: "5px",
    },
}
export default noticeStyle;